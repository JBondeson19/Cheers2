<h1>All Drinks</h1>

<%= link_to 'Back To User Page', user_path(session[:user_id])%>

<% @drinks.each do |drink|%>
<ul>
    <li><%= link_to drink.name, drink_path(drink.id)%> -- <%= drink.classification %></li>
</ul>
<% end %>

<%= link_to 'Logout', logout_path, method: 'delete'%>